[workspace]
members = ["client", "server"]
# `legacy/` is its own workspace: it enables `legacy-spec` on the shared `neva`
# path dependency, and Cargo would unify that feature onto these members if they
# were built together.
exclude = ["legacy"]
resolver = "2"

[workspace.package]
rust-version = "1.90.0"
