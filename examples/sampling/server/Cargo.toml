[package]
name = "server"
version = "0.1.0"
edition = "2024"
publish = false
rust-version.workspace = true

[dependencies]
neva = { path = "../../../neva", features = ["server-full"] }
tokio = { version = "1.44.1", features = ["full"] }
