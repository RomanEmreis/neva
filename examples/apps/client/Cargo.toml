[package]
name = "client"
version = "0.1.0"
edition = "2024"
publish = false
rust-version.workspace = true

[dependencies]
neva = { path = "../../../neva", features = ["client", "apps"] }
tokio = { version = "1.47.1", features = ["full"] }
