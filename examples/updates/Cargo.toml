[package]
name = "example-updates"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
neva = { path = "../../neva", features = ["server-macros", "tracing"] }
tokio = { version = "1.47.1", features = ["full"] }